<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/productSpecificationName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textSize="@dimen/textSize"
        android:textStyle="bold"
        android:textColor="@color/colorSecondaryText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintWidth_percent="0.7"
        app:layout_constraintWidth_max="wrap"
        tools:text="Количество режимов работы"/>

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/productSpecificationDivider"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textSize="@dimen/textSize"
        android:textStyle="bold"
        android:textColor="@color/colorSecondaryText"
        android:maxLines="1"
        app:layout_constraintStart_toEndOf="@id/productSpecificationName"
        app:layout_constraintEnd_toStartOf="@id/productSpecificationValue"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:text=" ................."/>

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/productSpecificationValue"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textSize="@dimen/textSize"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintWidth_percent="0.14"
        tools:text="4"/>

</androidx.constraintlayout.widget.ConstraintLayout>